<!DOCTYPE html>
<html lang="{{ pagevars.lang }}" dir="{{ pagevars.langdirection }}">
    <head>
        <meta charset="{{ pagevars.meta.charset }}" />
        <title>{{ pagevars.title }}</title>
        <meta name="description" content="{{ pagevars.meta.description }}" />
        <meta name="keywords" content="{{ pagevars.meta.keywords }}" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />        
        {{ pageAddAsset('stylesheet', zasset('@ZikulaAdminLTETheme:css/style.css')) }}
		<link href=" {{ pageAddAsset('stylesheet', zasset('@ZikulaAdminLTETheme:adminlte/dist/css/AdminLTE.min.css'), 98) }}" rel="stylesheet" type="text/css" />
        <!-- AdminLTE Skins. We have chosen the skin-blue for this starter
              page. However, you can choose any other skin. Make sure you
              apply the skin class to the body tag so the changes take effect.
        -->
        <link href="{{ pageAddAsset('stylesheet', zasset('@ZikulaAdminLTETheme:adminlte/dist/css/skins/skin-blue.min.css'), 98)  }}" rel="stylesheet" type="text/css" />

        
    </head>
<body class="hold-transition skin-blue sidebar-mini">
<div class="wrapper">

  <header class="main-header">
    <!-- Logo -->
    <a href="{{ pagevars.homepath }}" class="logo">
      <!-- mini logo for sidebar mini 50x50 pixels -->
      <span class="logo-mini"><img src="{{ zasset('@ZikulaAdminLTETheme:images/sygnet.png') }}" width="40px" height="40px"/></span>
      <!-- logo for regular state and mobile devices -->
      <span class="logo-lg"><img src="{{ zasset('@ZikulaAdminLTETheme:images/logo.png') }}" height="40px"/></span>
    </a>
    
    <!-- Header Navbar: style can be found in header.less -->
    <nav class="navbar navbar-static-top" role="navigation">
      <!-- Sidebar toggle button-->
      <a href="#" class="sidebar-toggle" data-toggle="offcanvas" role="button">
        <span class="sr-only">{{ __('Toggle navigation') }}</span>
      </a>
      {% include 'ZikulaAdminLTETheme:Include:admin.status_bar.html.twig' %}
    </nav>
  </header>
