<!DOCTYPE html>
<html lang="{{ pagevars.lang }}" dir="{{ pagevars.langdirection }}">
    <head>
        <meta charset="{{ pagevars.meta.charset }}" />
        <title>{{ pagevars.title }}</title>
        <meta name="description" content="{{ pagevars.meta.description }}" />
        <meta name="keywords" content="{{ pagevars.meta.keywords }}" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />        
        {{ pageAddAsset('stylesheet', zasset('@KaikmediaAdminLTETheme:css/style.css')) }}
        {#            {% block title %}{{ pageSetVar('title', title|default('Base title')) }}{% endblock %}     #}
        {% block head %}
        {% endblock %}
    </head>
    {% block body %}<body>
        {% endblock %}
        {% block content %}
        {% endblock %}
        {% block footer %}
        {% endblock %}
    </body>
</html>